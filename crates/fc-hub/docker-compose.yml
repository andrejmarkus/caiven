services:
  hub:
    build:
      context: ../..
      dockerfile: crates/fc-hub/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - hub-data:/data
    restart: unless-stopped

volumes:
  hub-data:
