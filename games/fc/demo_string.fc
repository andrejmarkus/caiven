-- String demo: static string literals via txt()

let frame = 0

loop:
  cls()
  txt("hello, world!", 2, 2, 7)
  txt("fc-lang strings", 2, 12, 6)
  txt("foo" .. "bar", 2, 22, 5)
  frame += 1
  wait()
